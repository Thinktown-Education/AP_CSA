# AP CSA Homework: 2D Array Practice

Assume all 2D arrays are **rectangular** (same number of columns in every row).


## Part A: Reading & Tracing Code

### 1.

```java
int[][] nums = {
    {1, 2, 3},
    {4, 5, 6}
};

for (int r = 0; r < nums.length; r++) {
    for (int c = 0; c < nums[0].length; c++) {
        System.out.print(nums[r][c] + " ");
    }
}
```

**What is printed?**

---

### 2.

```java
int[][] data = {
    {2, 4},
    {6, 8},
    {10, 12}
};

int sum = 0;

for (int r = 0; r < data.length; r++) {
    sum += data[r][1];
}

System.out.println(sum);
```

**What is printed?**

---

### 3.

```java
int[][] grid = {
    {5, 3, 1},
    {2, 4, 6}
};

for (int c = 0; c < grid[0].length; c++) {
    System.out.print(grid[0][c] + " ");
}
```

**What is printed?**

Part B: Writing Code

Complete Q1 - Q7 in [2D array practice](./hw_20260120.java)
